[project]
name = "nicokara-studio-next-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.116,<1", "pydantic-settings>=2.10,<3", "python-multipart>=0.0.20,<1", "uvicorn[standard]>=0.35,<1"]
[project.optional-dependencies]
dev = ["pytest>=8.0,<9", "httpx2>=2,<3"]
[tool.pytest.ini_options]
pythonpath = ["."]
